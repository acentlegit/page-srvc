# Backend API Configuration
# Change these values to switch between local backend and client backend

# API Base URL - For local testing use localhost, for client use their OpenAPI URL
VITE_API_BASE_URL=http://localhost:3000
VITE_API_PATH_PREFIX=/api

# For Client Backend (Staging - from Swagger):
# VITE_API_BASE_URL=https://cudb-root-api.staging.beamdev.hu
# VITE_API_PATH_PREFIX=

# API Endpoint Paths (Event Names) - Read from Swagger/OpenAPI documentation
# These are the endpoint names/paths for each resource
VITE_API_ENDPOINT_PAGES=/pages
VITE_API_ENDPOINT_EVENTS=/events
VITE_API_ENDPOINT_USERS=/createUser
VITE_API_ENDPOINT_MESSAGES=/messages

# API Topic Header (for Swagger API - if required)
# VITE_API_TOPIC=users

# When switching to client backend, update these if their endpoints are different:
# VITE_API_ENDPOINT_PAGES=/createPage
# VITE_API_ENDPOINT_EVENTS=/createEvent
# VITE_API_ENDPOINT_USERS=/createUser

# MQTT Configuration
VITE_MQTT_BROKER_URL=ws://localhost:9001
VITE_MQTT_QUEUE_NAME=beam-pages
VITE_MQTT_CLIENT_ID=beam-admin-frontend
VITE_MQTT_SUBSCRIPTION_TOPIC=pages/+

# MQTT Authentication (if required)
# VITE_MQTT_USERNAME=your_username
# VITE_MQTT_PASSWORD=your_password

# Page Service Configuration
VITE_PAGE_SERVICE_URL=http://localhost:3000/api/pages

# When switching to client backend, update:
# VITE_PAGE_SERVICE_URL=https://client-backend-url.com/api/pages
